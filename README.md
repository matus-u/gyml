# gyml
Generic yaml manipulator library

This small library can be used to obtain/manipulate any yaml file withouth knowing full structure of the file.
To access/change of the internal data fields, path based lookup is used.

## Usage
